{
  "name": "${{ values.repoName }}",
  "version": "1.0.0",
  "description": "${{ values.description }}",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}