apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.repoName }}
  description: ${{ values.description }}
  annotations:
    github.com/project-slug: pkredhat/${{ values.repoName }}
    che.eclipse.org/devworkspace-link: https://devspaces.apps.cluster-ffz4s.ffz4s.sandbox3066.opentlc.com/#https://github.com/pkredhat/${{ values.repoName }}
spec:
  type: service
  lifecycle: experimental  
